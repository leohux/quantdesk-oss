# GitHub metadata for quantdesk-oss
# CI workflow omitted from the initial public snapshot (OAuth token lacks `workflow` scope).
# Add `.github/workflows/ci.yml` from your private mirror when publishing with a token that has workflow permission.
